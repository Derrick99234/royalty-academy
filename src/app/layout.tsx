import type { Metadata } from "next";
import { Montserrat, Inter, Public_Sans } from "next/font/google";
import "./globals.css";

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const publicSans = Public_Sans({
  variable: "--font-public-sans",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Royalty Academy | Equipping and Empowering Money Missionaries",
  description:
    "Royalty Academy, founded by Dr. Isaiah Macwealth, is a leading institution in Lagos, Nigeria dedicated to empowering entrepreneurs, career enthusiasts, and business professionals through practical business education and skill acquisition.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${montserrat.variable} ${inter.variable} ${publicSans.variable} antialiased`}
    >
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="bg-background text-on-background font-body">
        {children}
      </body>
    </html>
  );
}
